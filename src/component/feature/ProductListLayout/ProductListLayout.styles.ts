import { css } from '@emotion/react';

export const productListLayoutStyle = css`
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 2.4rem;
  width: 100%;
  height: 100%;
  padding: 2.4rem;
  box-sizing: border-box;
`;
