import { describe, it, expect } from "vitest";
import { changeToProductList, calculatePaymentPrice } from "@/utils";

describe("util 함수 테스트", () => {
  it("장바구니 결제 금액 계산", () => {
    expect(1 + 1).toBe(2);
  });

  it("tanstack query의 무한 스크롤 데이터를 UI에 보여줄 수 있도록 알맞게 가공", () => {
    expect(1 + 1).toBe(2);
  });
});
