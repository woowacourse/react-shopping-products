/**
 * @name PAGE_OFFSET
 * 0페이지 요청 후 1페이지를 요청하면 중복 데이터가 발생하므로, 다음 페이지는 5부터 요청하도록 page에 4를 더해 요청하기 위한 상수
 */
export const PAGE_OFFSET = 4;
export const DEFAULT_SIZE = 20;
export const PAGE_PER_SIZE = 4;
