const useToggleCartItem = () => {
  return {
    cartItems: [],
    addToCart: (id: number) => {
      id;
    },
    removeFromCart: (id: number) => {
      id;
    },
    isLoading: false,
    error: null,
  };
};

export default useToggleCartItem;
