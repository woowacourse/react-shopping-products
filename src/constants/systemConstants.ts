export const PRODUCT_LIST_ITEM_COUNT = 20;

export const CATEGORY_OPTIONS = ["전체", "식료품", "패션잡화"];

export const SELECT_SORT_OPTIONS = ["높은 가격순", "낮은 가격순"];

export const SORT_OPTIONS = new Map<string, string>([
	["높은 가격순", "desc"],
	["낮은 가격순", "asc"],
]);

export const DEFAULT_PAGE = 0;
export const DEFAULT_SIZE = 20;

export const ERROR_MESSAGE_DURATION = 3000;
export const ERROR_MESSAGE_ANIMATION_DELAY = 200;
