import styled from "@emotion/styled";

export const Select = styled.select`
  width: 125px;
  height: 36px;
  border: 1px solid rgba(0, 0, 0, 0.1);
  border-radius: 8px;
  font-size: 14px;
  padding: 0 0 0 8px;
`;
