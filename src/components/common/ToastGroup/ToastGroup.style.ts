import styled from 'styled-components';

export const ToastGruop = styled.div`
  position: fixed;
  width: 100%;
  max-width: 640px;
  display: flex;
  flex-direction: column;
  padding: 16px;
  gap: 8px;

  top: 64px;
  right: 0;
`;
