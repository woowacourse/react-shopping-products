import styled from 'styled-components';

export const NavigationBar = styled.header`
  position: fixed;
  top: 0;
  width: 430px;
  height: 64px;
  padding: 0 24px;
  background: #000000;
  z-index: 100;

  display: flex;
  align-items: center;

  color: #ffffff;
  font-size: 20px;
  font-weight: 800;
`;
